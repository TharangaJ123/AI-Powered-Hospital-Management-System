package com.sliit.hospitalManagementSystem.doctor_management.model;

public enum ConsultationType {
    // Physical meeting at the hospital or clinic
    IN_PERSON,
    // Virtual consultation via video call
    TELEMEDICINE,
    // Flexible mode supporting both physical and virtual options
    BOTH
}
